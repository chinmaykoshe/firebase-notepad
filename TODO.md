# TODO

- [ ] Update paste handling in `src/App.jsx` so clipboard HTML that contains both Word paragraphs and `<table>` inserts:
  - non-table content as normal word/text
  - table content separately as real tables
  - tables and text in a reasonable order based on DOM position
- [ ] Run dev build/test command to ensure no syntax errors (vite dev/build)

